// /dashboard (protected in future) shown after the user logs in or signs up.

export default function Dashboard() {
  return (
    <main style={{ display: "grid", placeItems: "center", height: "70vh" }}>
      <h1>Dashboard</h1>
    </main>
  );
}